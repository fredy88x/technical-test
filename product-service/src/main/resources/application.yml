server:
  port: 8083
spring:
  datasource:
    url: jdbc:mysql://product-db.c9gkymu6mcxm.us-east-1.rds.amazonaws.com:3306/product_db
    username: root
    password: admintest
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format-sql: true


logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
      type:
        descriptor:
          sql:
            BasicBinder: TRACE
